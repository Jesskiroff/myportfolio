// Hero typically represents the primary section above the fold, once one visits the page.
import React from 'react';

const Hero = () => {
  return (
  <div className='pb-20 pt-36'>Hero</div>
)
};

export default Hero;
